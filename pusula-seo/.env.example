# Copy this file to .env for local testing. Never commit .env — it is gitignored.
# In production (the cloud Routine), these are set as environment variables on
# the Routine's environment config, not committed here either.

# --- Telegram (required for the AI assistant) ---
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# --- Google Search Console (Phase 3 — leave blank until the service account is added as a property user) ---
# Paste the full service-account JSON key as a single-line string, or a path to the key file.
GSC_SERVICE_ACCOUNT_JSON=
# Must exactly match how the property is registered in Search Console:
#   Domain property      -> sc-domain:centaurstudios.online
#   URL-prefix property  -> https://centaurstudios.online/
# Check Search Console's property switcher (top-left dropdown) to see which
# one this is — all GSC queries are already filtered to /pusula/ pages only,
# regardless of which property type this is (the property is domain-wide).
GSC_SITE_URL=

# --- GA4 Data API (Phase 3) ---
GA4_SERVICE_ACCOUNT_JSON=
GA4_PROPERTY_ID=

# --- PostHog API (Phase 3 — separate from the client-side snippet key) ---
POSTHOG_API_KEY=
POSTHOG_PROJECT_ID=
POSTHOG_HOST=https://eu.posthog.com

# --- Telegram webhook accelerator (only needed if deploying the optional Cloudflare Worker) ---
TELEGRAM_WEBHOOK_SECRET=
GITHUB_CONTENTS_TOKEN=
